FROM node:22-alpine AS base
ENV PNPM_HOME="/pnpm"
ENV PATH="$PNPM_HOME:$PATH"
RUN corepack enable
WORKDIR /api
COPY package.json pnpm-lock.yaml ./
RUN pnpm install
EXPOSE 3000
CMD ["pnpm", "run", "dev"]
